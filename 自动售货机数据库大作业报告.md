# 自动售货机管理系统 — 数据库课程大作业报告

> **技术栈**：Python 3.x + MySQL (pymysql)
>
> **班级**：软件12403班 &nbsp;&nbsp; **姓名**：常向阳 &nbsp;&nbsp; **学号**：2404240920

---

## 目录

1. [需求分析](#一需求分析)
2. [系统设计](#二系统设计)
3. [系统实现](#三系统实现)
4. [测试和评估](#四测试和评估)
5. [项目总结](#五项目总结)

---

## 一、需求分析

### 1.1 业务背景

随着物联网与移动支付的普及，自动售货机已成为校园、办公楼、地铁站等公共场所的重要零售终端。传统的人工管理方式效率低下，难以实时掌握库存、销售数据和设备状态。因此，开发一套基于数据库的自动售货机管理系统，实现商品管理、库存监控、订单交易等功能，具有重要的现实意义。

本系统采用 B/S 架构思想，基于 Python + MySQL 实现，面向**管理员**和**普通用户**两类角色，提供完整的售货机运营管理功能。

### 1.2 核心功能需求

#### 1.2.1 管理员功能

| 功能模块 | 功能描述 |
|---------|---------|
| 商品管理 | 添加、修改、删除、查看商品信息（名称、分类、描述、单价） |
| 售货机管理 | 添加售货机、修改售货机状态（在线/离线/维护中）、查看全部售货机 |
| 库存管理 | 为售货机补充商品库存、直接设置库存数量、查看指定售货机的库存情况 |
| 订单查看 | 查看系统中所有订单记录（含用户名、售货机名、金额、时间） |
| 用户查看 | 查看系统中所有注册用户及其余额 |

#### 1.2.2 普通用户功能

| 功能模块 | 功能描述 |
|---------|---------|
| 用户注册/登录 | 新用户注册账号，已有用户通过用户名密码登录 |
| 账户充值 | 用户为自己的账户充值余额 |
| 浏览商品 | 查看在线售货机及其中的商品（含名称、单价、库存） |
| 购买商品 | 选择售货机和商品并下单，系统在**数据库事务**中完成扣款、减库存、生成订单 |
| 订单查询 | 查看自己的历史订单列表及每条订单的详细信息（含商品明细） |
| 修改密码 | 修改登录密码 |

### 1.3 数据需求

系统需要持久化存储以下核心数据：

1. **用户数据**：用户名、密码（加密存储）、角色、余额、手机号、邮箱、注册时间
2. **售货机数据**：名称、摆放位置、运行状态、最大容量
3. **商品数据**：名称、分类、描述、单价
4. **库存数据**：某售货机中某商品的当前数量和最大容量（多对多关系）
5. **订单数据**：下单用户、售货机、总金额、状态、下单时间
6. **订单明细数据**：每个订单中每种商品的数量和购买时单价

### 1.4 非功能需求

| 需求类别 | 具体要求 |
|---------|---------|
| 数据一致性 | 订单创建过程必须使用**数据库事务**，确保扣款、减库存、生成订单的原子性，失败时完整回滚 |
| 数据完整性 | 通过主键约束、外键约束、唯一约束保证数据不重复、不孤立 |
| 并发安全 | 在事务中对用户余额行和库存行使用 `SELECT ... FOR UPDATE` 行锁，防止并发超卖 |
| 安全性 | 密码使用 SHA256 哈希存储，不保存明文 |
| 易用性 | 命令行交互界面清晰，提示信息友好，可直接运行 |
| 可移植性 | 代码只需修改数据库连接配置即可运行于任意 MySQL 实例 |

---

## 二、系统设计

### 2.1 概念结构设计（E-R 图）

系统包含 **5 个实体** 和 **1 个多对多关系转换的关联实体**，共形成 6 张表。

#### 实体及其属性

**（1）用户实体（User）**

![用户E-R图](https://via.placeholder.com/600x200/FFFFFF/000000?text=用户实体：用户ID+用户名+密码+角色+余额+手机号+邮箱+注册时间)

- **主键**：用户ID（user_id）
- **属性**：用户名(username)、密码(password)、角色(role)、余额(balance)、手机号(phone)、邮箱(email)、注册时间(created_at)

**（2）售货机实体（VendingMachine）**

- **主键**：售货机ID（machine_id）
- **属性**：名称(name)、位置(location)、状态(status)、容量(capacity)、创建时间(created_at)

**（3）商品实体（Product）**

- **主键**：商品ID（product_id）
- **属性**：名称(name)、分类(category)、描述(description)、单价(price)、图片URL(image_url)、创建时间(created_at)

**（4）订单实体（Order）**

- **主键**：订单ID（order_id）
- **属性**：总金额(total_amount)、状态(status)、创建时间(created_at)
- **外键**：用户ID(user_id)→用户表、售货机ID(machine_id)→售货机表

**（5）订单明细实体（OrderDetail）**

- **主键**：明细ID（detail_id）
- **属性**：数量(quantity)、购买时单价(unit_price)
- **外键**：订单ID(order_id)→订单表、商品ID(product_id)→商品表

#### 实体间关系

```
                    ┌──────────┐
                    │  用户    │
                    │  User   │
                    └────┬─────┘
                         │ 1
                         │
                         │ N
                    ┌────▼─────┐       ┌──────────┐
                    │  订单    │ N : 1 │ 售货机   │
                    │  Order  │◄──────│ Machine  │
                    └────┬─────┘       └────┬─────┘
                         │ 1                │
                         │                  │ N:M (通过库存关联)
                         │ N                │
                    ┌────▼─────┐       ┌────▼─────┐
                    │ 订单明细 │       │  库存    │
                    │OrderDetail│      │Inventory │
                    └────┬─────┘       └────┬─────┘
                         │ N                │ N
                         │                  │
                         │ 1                │ 1
                    ┌────▼─────┐       ┌────▼─────┐
                    │  商品    │◄──────│  商品    │
                    │ Product  │       │ Product  │
                    └──────────┘       └──────────┘
```

#### 关系详细说明

| 关系 | 类型 | 说明 |
|------|------|------|
| 用户 ↔ 订单 | 1 : N | 一个用户可以下多个订单；一个订单只属于一个用户 |
| 售货机 ↔ 订单 | 1 : N | 一台售货机可以关联多个订单；一个订单只对应一台售货机 |
| 订单 ↔ 订单明细 | 1 : N | 一个订单可包含多种商品（多条明细）；每条明细属于一个订单 |
| 商品 ↔ 订单明细 | 1 : N | 一种商品可出现在多条订单明细中；每条明细记录一种商品 |
| 售货机 ↔ 商品（库存） | N : M | 一台售货机可存放多种商品；一种商品可存放在多台售货机中。通过**库存表(Inventory)** 转换为两个 1:N 关系 |

### 2.2 逻辑结构设计（MySQL 表结构）

#### 2.2.1 用户表（users）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| user_id | INT | **PRIMARY KEY**, AUTO_INCREMENT | 用户唯一标识 |
| username | VARCHAR(50) | **NOT NULL**, **UNIQUE** | 登录用户名 |
| password | VARCHAR(255) | NOT NULL | SHA256 哈希后的密码 |
| role | ENUM('user','admin') | NOT NULL, DEFAULT 'user' | 用户角色 |
| balance | DECIMAL(10,2) | NOT NULL, DEFAULT 0.00 | 账户余额（元） |
| phone | VARCHAR(20) | DEFAULT NULL | 手机号码 |
| email | VARCHAR(100) | DEFAULT NULL | 电子邮箱 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 注册时间 |

```sql
CREATE TABLE IF NOT EXISTS users (
    user_id     INT AUTO_INCREMENT PRIMARY KEY COMMENT '用户唯一ID',
    username    VARCHAR(50)  NOT NULL UNIQUE COMMENT '登录用户名',
    password    VARCHAR(255) NOT NULL COMMENT 'SHA256哈希后的密码',
    role        ENUM('user','admin') NOT NULL DEFAULT 'user'
                COMMENT '角色：user-普通用户 / admin-管理员',
    balance     DECIMAL(10,2) NOT NULL DEFAULT 0.00 COMMENT '账户余额（元）',
    phone       VARCHAR(20)  DEFAULT NULL COMMENT '手机号码',
    email       VARCHAR(100) DEFAULT NULL COMMENT '电子邮箱',
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '注册时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='用户表';
```

**设计说明**：
- `role` 使用 ENUM 类型限制取值，避免非法角色插入
- `balance` 使用 DECIMAL(10,2) 而非 FLOAT，避免浮点精度误差（金融数据的关键要求）
- `password` 存储 SHA256 哈希值，不存明文

#### 2.2.2 售货机表（vending_machines）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| machine_id | INT | **PRIMARY KEY**, AUTO_INCREMENT | 售货机唯一标识 |
| name | VARCHAR(100) | NOT NULL | 售货机名称 |
| location | VARCHAR(255) | DEFAULT NULL | 摆放位置 |
| status | ENUM('online','offline','maintenance') | NOT NULL, DEFAULT 'online' | 运行状态 |
| capacity | INT | NOT NULL, DEFAULT 100 | 最大货道容量 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

```sql
CREATE TABLE IF NOT EXISTS vending_machines (
    machine_id  INT AUTO_INCREMENT PRIMARY KEY COMMENT '售货机唯一ID',
    name        VARCHAR(100) NOT NULL COMMENT '售货机名称',
    location    VARCHAR(255) DEFAULT NULL COMMENT '摆放位置',
    status      ENUM('online','offline','maintenance')
                NOT NULL DEFAULT 'online'
                COMMENT '状态：online-在线 / offline-离线 / maintenance-维护中',
    capacity    INT NOT NULL DEFAULT 100 COMMENT '最大货道容量（件）',
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='售货机表';
```

**设计说明**：
- `status` 使用 ENUM 类型，语义清晰，防止脏数据
- `capacity` 表示该售货机能容纳的商品总件数上限

#### 2.2.3 商品表（products）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| product_id | INT | **PRIMARY KEY**, AUTO_INCREMENT | 商品唯一标识 |
| name | VARCHAR(100) | NOT NULL | 商品名称 |
| category | VARCHAR(50) | DEFAULT NULL | 商品分类（如饮料、零食） |
| description | TEXT | DEFAULT NULL | 商品描述 |
| price | DECIMAL(10,2) | NOT NULL | 商品单价（元） |
| image_url | VARCHAR(255) | DEFAULT NULL | 商品图片URL |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 创建时间 |

```sql
CREATE TABLE IF NOT EXISTS products (
    product_id  INT AUTO_INCREMENT PRIMARY KEY COMMENT '商品唯一ID',
    name        VARCHAR(100) NOT NULL COMMENT '商品名称',
    category    VARCHAR(50)  DEFAULT NULL COMMENT '商品分类',
    description TEXT          DEFAULT NULL COMMENT '商品描述',
    price       DECIMAL(10,2) NOT NULL COMMENT '商品单价（元）',
    image_url   VARCHAR(255) DEFAULT NULL COMMENT '商品图片URL',
    created_at  TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '创建时间'
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='商品表';
```

**设计说明**：
- `price` 同样使用 DECIMAL(10,2) 而非 FLOAT，确保金额计算精确
- `description` 使用 TEXT 类型以支持较长的商品说明

#### 2.2.4 库存表（inventory）— 多对多关系转换

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| inventory_id | INT | **PRIMARY KEY**, AUTO_INCREMENT | 库存记录ID |
| machine_id | INT | **NOT NULL**, **FOREIGN KEY** → vending_machines(machine_id) | 售货机ID |
| product_id | INT | **NOT NULL**, **FOREIGN KEY** → products(product_id) | 商品ID |
| quantity | INT | NOT NULL, DEFAULT 0 | 当前库存数量 |
| max_quantity | INT | NOT NULL, DEFAULT 50 | 该货道最大容量 |

```sql
CREATE TABLE IF NOT EXISTS inventory (
    inventory_id INT AUTO_INCREMENT PRIMARY KEY COMMENT '库存记录ID',
    machine_id   INT NOT NULL COMMENT '售货机ID（外键）',
    product_id   INT NOT NULL COMMENT '商品ID（外键）',
    quantity     INT NOT NULL DEFAULT 0 COMMENT '当前库存数量',
    max_quantity INT NOT NULL DEFAULT 50 COMMENT '该货道最大容量',
    FOREIGN KEY (machine_id) REFERENCES vending_machines(machine_id)
        ON DELETE CASCADE,
    FOREIGN KEY (product_id) REFERENCES products(product_id)
        ON DELETE CASCADE,
    UNIQUE KEY uk_machine_product (machine_id, product_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='库存表（售货机与商品的多对多关系）';
```

**设计说明**：
- 这是**多对多关系的核心实现**：一台售货机可以存放多种商品，一种商品可以出现在多台售货机中，因此引入库存表作为关联实体
- `UNIQUE KEY (machine_id, product_id)` 保证同一售货机中同一种商品只有一条库存记录
- `ON DELETE CASCADE`：售货机或商品被删除时，对应的库存记录自动清除
- 将 `quantity` 与 `max_quantity` 分开存储，便于实现库存预警（当前未实现，预留扩展）

#### 2.2.5 订单表（orders）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| order_id | INT | **PRIMARY KEY**, AUTO_INCREMENT | 订单唯一标识 |
| user_id | INT | **NOT NULL**, **FOREIGN KEY** → users(user_id) | 下单用户ID |
| machine_id | INT | **NOT NULL**, **FOREIGN KEY** → vending_machines(machine_id) | 售货机ID |
| total_amount | DECIMAL(10,2) | NOT NULL | 订单总金额 |
| status | ENUM('pending','completed','cancelled') | NOT NULL, DEFAULT 'pending' | 订单状态 |
| created_at | TIMESTAMP | DEFAULT CURRENT_TIMESTAMP | 下单时间 |

```sql
CREATE TABLE IF NOT EXISTS orders (
    order_id     INT AUTO_INCREMENT PRIMARY KEY COMMENT '订单唯一ID',
    user_id      INT NOT NULL COMMENT '下单用户ID（外键）',
    machine_id   INT NOT NULL COMMENT '售货机ID（外键）',
    total_amount DECIMAL(10,2) NOT NULL COMMENT '订单总金额（元）',
    status       ENUM('pending','completed','cancelled')
                 NOT NULL DEFAULT 'pending' COMMENT '订单状态',
    created_at   TIMESTAMP DEFAULT CURRENT_TIMESTAMP COMMENT '下单时间',
    FOREIGN KEY (user_id) REFERENCES users(user_id),
    FOREIGN KEY (machine_id) REFERENCES vending_machines(machine_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单表';
```

**设计说明**：
- `total_amount` 冗余存储订单总金额，避免每次查询都需要 JOIN 明细表求和
- `status` 支持三种状态，当前系统主要使用 `completed`（事务成功）和 `pending`（预留）

#### 2.2.6 订单明细表（order_details）

| 字段名 | 类型 | 约束 | 说明 |
|--------|------|------|------|
| detail_id | INT | **PRIMARY KEY**, AUTO_INCREMENT | 明细唯一标识 |
| order_id | INT | **NOT NULL**, **FOREIGN KEY** → orders(order_id) | 所属订单ID |
| product_id | INT | **NOT NULL**, **FOREIGN KEY** → products(product_id) | 商品ID |
| quantity | INT | NOT NULL | 购买数量 |
| unit_price | DECIMAL(10,2) | NOT NULL | 购买时的商品单价 |

```sql
CREATE TABLE IF NOT EXISTS order_details (
    detail_id  INT AUTO_INCREMENT PRIMARY KEY COMMENT '明细唯一ID',
    order_id   INT NOT NULL COMMENT '订单ID（外键）',
    product_id INT NOT NULL COMMENT '商品ID（外键）',
    quantity   INT NOT NULL COMMENT '购买数量',
    unit_price DECIMAL(10,2) NOT NULL COMMENT '购买时单价（元）',
    FOREIGN KEY (order_id) REFERENCES orders(order_id) ON DELETE CASCADE,
    FOREIGN KEY (product_id) REFERENCES products(product_id)
) ENGINE=InnoDB DEFAULT CHARSET=utf8mb4 COMMENT='订单明细表';
```

**设计说明**：
- `unit_price` 记录的是**购买时的商品单价**，而非当前商品价格。这是一种重要的数据库设计实践——即使日后商品价格变动，历史订单的金额依然准确
- `ON DELETE CASCADE` 在 order_id 外键上：删除订单时，其明细同时自动删除

### 2.3 索引设计

| 表名 | 索引类型 | 索引字段 | 用途 |
|------|---------|---------|------|
| users | UNIQUE | username | 登录时快速查找用户 |
| inventory | UNIQUE | (machine_id, product_id) | 防止同一售货机重复添加同一商品 |
| inventory | FOREIGN KEY | machine_id | 按售货机查询库存 |
| inventory | FOREIGN KEY | product_id | 按商品查询库存分布 |
| orders | FOREIGN KEY | user_id | 按用户查询订单历史 |
| orders | FOREIGN KEY | machine_id | 按售货机查询订单 |
| order_details | FOREIGN KEY | order_id | 按订单查询明细 |
| order_details | FOREIGN KEY | product_id | 按商品统计销量 |

### 2.4 数据库引擎选择

全部表使用 **InnoDB** 引擎，原因如下：
1. **支持事务**：订单创建流程必须在事务中完成，MyISAM 不支持事务
2. **支持行级锁**：`SELECT ... FOR UPDATE` 依赖行锁机制防止并发冲突
3. **支持外键约束**：保证引用完整性，MyISAM 不支持外键
4. **崩溃恢复**：InnoDB 有 redo log，系统异常崩溃后可以自动恢复

---

## 三、系统实现

### 3.1 开发环境

| 组件 | 版本/说明 |
|------|----------|
| 操作系统 | Windows 11 / macOS / Linux |
| Python | 3.8 及以上 |
| MySQL | 5.7 及以上（或 MariaDB 10.3+） |
| Python 依赖 | pymysql（纯 Python 实现的 MySQL 驱动） |

### 3.2 项目文件结构

```
自动售货机/
├── vending_system.py                # 全部 Python 源代码（单文件）
└── 自动售货机数据库大作业报告.md      # 本报告
```

采用单文件架构便于作业提交和评审，代码按职责分为以下模块：

| 模块 | 行范围 | 职责 |
|------|--------|------|
| 数据库配置 | `DB_CONFIG` | MySQL 连接参数，用户只需修改此处 |
| Database 类 | `class Database` | 连接管理、数据库/表创建、示例数据初始化 |
| UserDAO | `class UserDAO` | 用户注册、登录、充值、查询 |
| ProductDAO | `class ProductDAO` | 商品 CRUD |
| MachineDAO | `class MachineDAO` | 售货机管理 |
| InventoryDAO | `class InventoryDAO` | 库存管理（多对多关系维护） |
| OrderDAO | `class OrderDAO` | **核心**：事务型订单创建、订单查询 |
| CLI 界面 | `admin_menu()` / `user_menu()` | 管理员和用户的命令行交互界面 |

### 3.3 数据库连接实现

```python
class Database:
    def get_raw_connection(self):
        """获取不指定数据库的裸连接（用于创建数据库）"""
        return pymysql.connect(**DB_CONFIG)

    def get_connection(self):
        """获取已指定数据库的连接，每次调用返回新连接"""
        cfg = DB_CONFIG.copy()
        cfg["database"] = DATABASE_NAME
        return pymysql.connect(**cfg)
```

- 每次操作获取新连接并在 `finally` 中关闭，避免连接泄漏
- 使用 `pymysql` 的默认游标，保证与 MySQL 原生行为一致

### 3.4 核心业务实现：带事务保障的订单创建

这是整个系统最核心的数据库操作，体现了事务 ACID 特性的实际应用：

```python
def create_order(self, user_id, machine_id, items):
    conn = self.db.get_connection()
    try:
        conn.begin()  # 显式开启事务

        with conn.cursor() as cur:
            # 步骤1：锁定用户余额行（FOR UPDATE 行锁）
            cur.execute(
                "SELECT balance FROM users WHERE user_id = %s FOR UPDATE",
                (user_id,)
            )

            # 步骤2：遍历购买清单，锁定库存行并计算总金额
            for product_id, qty in items:
                cur.execute(
                    "SELECT quantity FROM inventory "
                    "WHERE machine_id = %s AND product_id = %s FOR UPDATE",
                    (machine_id, product_id)
                )
                # 检查库存 → 累加金额

            # 步骤3：检查余额是否充足

            # 步骤4：扣减余额
            cur.execute(
                "UPDATE users SET balance = balance - %s WHERE user_id = %s",
                (total, user_id)
            )

            # 步骤5：逐项扣减库存
            for product_id, qty in stock_checks:
                cur.execute(
                    "UPDATE inventory SET quantity = quantity - %s "
                    "WHERE machine_id = %s AND product_id = %s",
                    (qty, machine_id, product_id)
                )

            # 步骤6：写入订单主表
            cur.execute(
                "INSERT INTO orders (...) VALUES (...)", (...)
            )
            order_id = cur.lastrowid

            # 步骤7：写入订单明细
            for product_id, qty in stock_checks:
                cur.execute(
                    "INSERT INTO order_details (...) VALUES (...)", (...)
                )

        conn.commit()  # 全部成功，提交事务
        return True, f"订单创建成功！#{order_id}"

    except Exception as e:
        conn.rollback()  # 任一步骤失败，完整回滚
        return False, f"订单创建失败（事务已回滚）：{e}"
```

#### 事务设计的关键技术点

| 技术点 | 实现方式 | 作用 |
|--------|---------|------|
| **原子性（Atomicity）** | `conn.begin()` ... `conn.commit()` / `conn.rollback()` | 7个步骤要么全部成功，要么全部撤销 |
| **隔离性（Isolation）** | `SELECT ... FOR UPDATE` 行锁 | 防止并发购买时出现超卖（两个用户同时读到库存=1） |
| **一致性（Consistency）** | 余额校验 + 库存校验在事务内完成 | 确保金额和库存不会出现负数 |
| **持久性（Durability）** | InnoDB 引擎 + commit | 事务提交后数据永久保存，即使系统崩溃 |

#### 事务回滚场景

| 场景 | 触发条件 | 回滚行为 |
|------|---------|---------|
| 用户不存在 | `SELECT balance ... FOR UPDATE` 返回空 | 立即回滚，余额和库存不变 |
| 商品不存在 | `SELECT ... FROM products` 返回空 | 立即回滚 |
| 库存不足 | `inv['quantity'] < qty` | 立即回滚 |
| 余额不足 | `balance < total` | 立即回滚 |
| 数据库异常 | `pymysql.Error` | except 块中回滚 |

### 3.5 关键技术实现说明

#### 密码安全存储

```python
def hash_password(pwd: str) -> str:
    return hashlib.sha256(pwd.encode()).hexdigest()
```
密码以 SHA256 哈希值存储，数据库中不保留明文。登录时将用户输入的密码进行同样哈希后与数据库比对。

#### 多表 JOIN 查询

库存查询（InventoryDAO.get_by_machine）使用了内连接：

```sql
SELECT i.*, p.name AS product_name, p.price, i.quantity, i.max_quantity
FROM inventory i
JOIN products p ON i.product_id = p.product_id
WHERE i.machine_id = %s
ORDER BY i.product_id
```

订单详情查询（OrderDAO.get_detail）使用了三层连接：

```sql
SELECT o.*, u.username, m.name AS machine_name
FROM orders o
JOIN users u ON o.user_id = u.user_id
JOIN vending_machines m ON o.machine_id = m.machine_id
WHERE o.order_id = %s
```

#### 幂等初始化

使用 `INSERT IGNORE INTO` 确保重复运行不会产生重复数据：

```sql
INSERT IGNORE INTO users (username, password, role, balance)
VALUES ('admin', '<hash>', 'admin', 0.00);
```

---

## 四、测试和评估

### 4.1 测试环境

| 项目 | 配置 |
|------|------|
| MySQL 版本 | 8.0 |
| Python 版本 | 3.11 |
| pymysql 版本 | 1.1.0 |
| 测试数据 | 系统初始化自动插入（2个用户、2台售货机、5种商品、8条库存记录） |

### 4.2 功能测试用例表

#### 4.2.1 用户注册与登录

| 用例编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|---------|--------|------|---------|---------|------|
| TC-01 | 新用户注册 | 用户名"zhangsan"，密码"123456" | 注册成功 | 注册成功 | ✓ PASS |
| TC-02 | 重复用户名注册 | 用户名"admin" | 提示"已被占用" | 提示"已被占用" | ✓ PASS |
| TC-03 | 正确登录 | 用户名"admin"，密码"admin123" | 登录成功，进入管理员界面 | 登录成功 | ✓ PASS |
| TC-04 | 错误密码登录 | 用户名"admin"，密码"wrong" | 提示"密码错误" | 提示"密码错误" | ✓ PASS |
| TC-05 | 不存在用户登录 | 用户名"nobody" | 提示"用户不存在" | 提示"用户不存在" | ✓ PASS |

#### 4.2.2 商品管理（管理员）

| 用例编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|---------|--------|------|---------|---------|------|
| TC-06 | 添加商品 | 名称"百事可乐"，分类"饮料"，单价4.00 | 添加成功 | 添加成功 | ✓ PASS |
| TC-07 | 添加商品（负单价） | 单价=-1 | 提示"单价必须大于0" | 提示"单价必须大于0" | ✓ PASS |
| TC-08 | 修改商品信息 | 修改商品1单价为4.00 | 修改成功 | 修改成功 | ✓ PASS |
| TC-09 | 删除有库存的商品 | 删除商品1（有关联库存） | 提示"请先清除库存" | 提示"请先清除库存" | ✓ PASS |
| TC-10 | 查看全部商品 | — | 列出所有商品及价格 | 正确列出 | ✓ PASS |

#### 4.2.3 库存管理（管理员）

| 用例编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|---------|--------|------|---------|---------|------|
| TC-11 | 为售货机补货 | 售货机1，商品1，数量10 | 库存增加10 | 库存30→40 | ✓ PASS |
| TC-12 | 直接设置库存 | 售货机1，商品1，数量50 | 库存变为50 | 库存变为50 | ✓ PASS |
| TC-13 | 设置负数库存 | 数量=-5 | 提示"不能为负数" | 提示"不能为负数" | ✓ PASS |
| TC-14 | 查看售货机库存 | 售货机ID=1 | 显示该机全部商品及库存 | 正确显示5种商品 | ✓ PASS |

#### 4.2.4 订单创建（事务核心测试）

| 用例编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|---------|--------|------|---------|---------|------|
| TC-15 | 正常购买 | test用户购买商品1(2件)+商品2(1件) | 订单创建成功，余额扣减9.00 | 成功，#1号订单 ¥9.00 | ✓ PASS |
| TC-16 | 余额不足 | test用户购买100件商品1 | 提示"余额不足"，事务回滚 | 事务回滚，余额和库存不变 | ✓ PASS |
| TC-17 | 库存不足 | 购买999件商品1 | 提示"库存不足"，事务回滚 | 事务回滚 | ✓ PASS |
| TC-18 | 空清单购买 | 不输入任何商品 | 提示"不能为空" | 提示"不能为空" | ✓ PASS |
| TC-19 | 购买不存在商品 | 购买商品999 | 提示"商品不存在"，事务回滚 | 事务回滚 | ✓ PASS |

#### 4.2.5 订单查询

| 用例编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|---------|--------|------|---------|---------|------|
| TC-20 | 查看我的订单 | test用户登录后查看 | 显示历史订单 | 正确显示 | ✓ PASS |
| TC-21 | 查看订单详情 | 订单号1 | 显示用户、售货机、商品明细 | 正确显示全部信息 | ✓ PASS |
| TC-22 | 管理员查看所有订单 | admin登录查看 | 显示全部用户的订单 | 正确显示 | ✓ PASS |

#### 4.2.6 账户充值

| 用例编号 | 测试项 | 输入 | 预期结果 | 实际结果 | 状态 |
|---------|--------|------|---------|---------|------|
| TC-23 | 正常充值 | 充值50元 | 余额增加50 | 成功 | ✓ PASS |
| TC-24 | 充值负数 | 充值-10元 | 提示"必须大于0" | 提示"必须大于0" | ✓ PASS |

### 4.3 事务回滚验证测试

这是本次大作业最关键的测试，验证数据库事务的 ACID 特性：

| 测试场景 | 操作 | 验证方法 | 结果 |
|---------|------|---------|------|
| 余额不足回滚 | test用户余额100元，尝试购买999元商品 | 事务失败后查询用户余额和库存，确认未变化 | ✓ 余额和库存均未变化 |
| 库存不足回滚 | 购买超过库存数量的商品 | 事务失败后查询库存，确认未减少 | ✓ 库存未减少 |
| 多表一致性 | 正常创建订单 | 查询 orders + order_details + users.balance + inventory.quantity，确认全部一致 | ✓ 四表数据一致 |

### 4.4 测试结论

通过 24 个测试用例的验证，系统在以下方面表现良好：

1. **功能完整性**：管理员和用户的所有核心功能均可正常使用
2. **数据一致性**：订单创建的事务机制可靠，异常情况下完整回滚，不会出现"钱扣了但库存没减"或"库存减了但订单没生成"的情况
3. **输入校验**：对非法输入（负数、空值、不存在的ID等）均有合理的拦截和提示
4. **并发安全性**：`SELECT ... FOR UPDATE` 行锁机制可在多用户同时购买时防止超卖

---

## 五、项目总结

### 5.1 项目完成情况

| 需求项 | 完成状态 | 说明 |
|--------|---------|------|
| 数据库设计 | ✓ 已完成 | 6张表，含完整字段、主键、外键、索引、约束 |
| 用户注册/登录 | ✓ 已完成 | 含密码哈希存储 |
| 商品管理（管理员） | ✓ 已完成 | 增删改查完整 |
| 售货机管理（管理员） | ✓ 已完成 | 添加、状态修改 |
| 库存管理（管理员） | ✓ 已完成 | 补货、设置、查看（JOIN 查询） |
| 订单创建（事务） | ✓ 已完成 | 7步事务流程 + 行锁 + 回滚 |
| 订单查询 | ✓ 已完成 | 历史订单 + 详情（含商品明细） |
| 账户充值 | ✓ 已完成 | — |
| 命令行交互界面 | ✓ 已完成 | 双角色菜单，清晰友好 |
| 测试用例 | ✓ 已完成 | 24个测试用例，覆盖正常/异常/边界 |
| 文档报告 | ✓ 已完成 | 含需求分析、ER设计、表结构、测试、总结 |

### 5.2 技术收获

1. **数据库设计能力**：掌握了从需求分析→概念模型（E-R图）→逻辑模型（表结构）的完整设计流程，理解了实体识别、关系建模、范式化的实践方法。

2. **事务编程能力**：深入理解了事务 ACID 特性在实际代码中的实现方式，包括 `BEGIN`/`COMMIT`/`ROLLBACK` 的显式控制，以及 `SELECT ... FOR UPDATE` 行锁的使用场景。

3. **多对多关系处理**：通过库存表（关联实体）将售货机与商品的多对多关系转换为两个一对多关系，是数据库范式化设计的典型实践。

4. **金融数据精度处理**：认识到 DECIMAL 类型在金额存储中的必要性，FLOAT/DOUBLE 的浮点误差在金融场景中不可接受。

5. **Python 数据库编程**：掌握了 pymysql 的连接管理、游标使用、参数化查询（防止 SQL 注入）、事务控制等实战技能。

### 5.3 不足与改进方向

| 不足 | 改进方向 |
|------|---------|
| 命令行界面不够直观 | 可改为 Web 界面（Flask/Django + HTML），或使用 Tkinter 实现 GUI |
| 未实现连接池 | 高并发场景下应使用 `DBUtils.PooledDB` 或 SQLAlchemy 的连接池 |
| 无日志记录 | 应使用 Python `logging` 模块记录操作日志，便于审计和排错 |
| 密码哈希简单 | 生产环境应使用 bcrypt/scrypt/argon2 等带盐哈希算法替代 SHA256 |
| 无真正的并发测试 | 可使用 Python 多线程模拟并发购买，验证 `FOR UPDATE` 锁的效果 |
| 库存预警缺失 | 可在库存表中增加 `alert_threshold` 字段，当 `quantity < alert_threshold` 时自动提醒管理员 |
| 缺少销售统计功能 | 可增加按商品/售货机/时间段的销售统计 SQL（GROUP BY + 聚合函数） |
| 无数据库备份恢复 | 生产系统应定时 `mysqldump` 或使用 MySQL 主从复制 |

---

## 附录：系统使用说明

### 快速运行

```bash
# 1. 安装依赖
pip install pymysql

# 2. 确保 MySQL 已启动

# 3. 修改 vending_system.py 中的 DB_CONFIG：
#    - user:     你的 MySQL 用户名
#    - password: 你的 MySQL 密码

# 4. 运行
python vending_system.py
```

### 预置账号

| 角色 | 用户名 | 密码 | 初始余额 |
|------|--------|------|---------|
| 管理员 | admin | admin123 | 0.00 |
| 普通用户 | test | test123 | 100.00 |

### 操作示例

```
# 管理员登录后：
#   商品管理 → 查看/添加/修改/删除商品
#   售货机管理 → 添加售货机/修改状态
#   库存管理 → 为售货机补货

# 用户登录后：
#   浏览售货机商品 → 选择售货机 → 查看商品和价格
#   购买商品 → 选择售货机 → 输入商品ID和数量 → 确认 → 事务执行
#   查看我的订单 → 显示历史订单列表

# 购买清单输入示例：
#   > 1 2    （商品1，买2件）
#   > 3 1    （商品3，买1件）
#   >         （空行结束）
```

---

> **声明**：本报告及代码为数据库课程大作业原创成果，独立完成。

---

*报告完成日期：2026年5月17日*
